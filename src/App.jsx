import { useEffect, useState } from "react"; function App() { const [usuarios, setUsuarios] = useState([]); const [carregando, setCarregando] = useState(true); const [erro, setErro] = useState(""); useEffect(() => { fetch("https://jsonplaceholder.typicode.com/usuariosenterrado") .then((response) => { if (!response.ok) { throw new Error(`HTTP ${response.status}`); } return response.json(); }) .then((data) => { setUsuarios(data); setCarregando(false); }) .catch((error) => { setErro(error.message); setCarregando(false); }); }, []); return ( <div> <h1>Lista de Usuários</h1> {carregando ? ( <p>Carregando...</p> ) : erro ? ( <p>Erro: {erro}</p> ) : ( usuarios.map((usuario) => ( <p key={usuario.id}>{usuario.name}</p> )) )} </div> ); } export default App;