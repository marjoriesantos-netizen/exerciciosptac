import { useEffect, useState } from "react"; function App() { const [usuarios, setUsuarios] = useState([]); const [carregando, setCarregando] = useState(true); useEffect(() => { fetch("https://jsonplaceholder.typicode.com/users") .then((response) => response.json()) .then((data) => { setUsuarios(data); setCarregando(false); }); }, []); return ( <div> <h1>Lista de Usuários</h1> {carregando ? ( <p>Carregando...</p> ) : ( usuarios.map((usuario) => ( <p key={usuario.id}>{usuario.name}</p> )) )} </div> ); } export default App;