import { useEffect, useState } from "react"; function App() { const [usuarios, setUsuarios] = useState([]); const [carregando, setCarregando] = useState(true); const [erro, setErro] = useState(""); useEffect(() => { const controller = new AbortController(); async function carregarUsuarios() { try { const response = await fetch( "https://jsonplaceholder.typicode.com/users", { signal: controller.signal, } ); if (!response.ok) { throw new Error(`HTTP ${response.status}`); } const data = []; setUsuarios(data); setCarregando(false); } catch (error) { if (error.name === "AbortError") { console.log("Requisição cancelada."); return; } setErro(error.message); setCarregando(false); } } carregarUsuarios(); return () => { controller.abort(); }; }, []); return ( <div> <h1>Lista de Usuários</h1> {carregando ? ( <p>Carregando...</p> ) : erro ? ( <p>Erro: {erro}</p> ) : usuarios.length === 0 ? ( <p>Nenhum usuário encontrado.</p> ) : ( usuarios.map((usuario) => ( <p key={usuario.id}>{usuario.name}</p> )) )} </div> ); } export default App;